# RDCover
---

RDCover, or Ruby Data Cover is a library for interfacing with SQL databases.
It makes the programmer's job of querying a database much less tedious with
Ruby methods, allowing easy lookup with little configuration.
